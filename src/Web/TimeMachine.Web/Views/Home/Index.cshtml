@model TimeMachine.Services.Models.Stories.StoriesViewModel
@{
    ViewData["Title"] = "Home Page";
}

<div class="container">
	<div class="list-group">
		@foreach (var userProfile in Model.Stories)
		{
			<a href="#" class="list-group-item list-group-item-action">@userProfile.Content</a>
		}
	</div>
</div>