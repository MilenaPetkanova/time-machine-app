@model TimeMachine.Web.Models.Home.IndexViewModel
@{
    ViewData["Title"] = "Home Page";
}

<div class="container">
	<div class="list-group">
		@foreach (var userProfile in Model.UserProfiles)
		{
			<a href="#" class="list-group-item list-group-item-action">@userProfile.UserFullName</a>
		}
	</div>
</div>