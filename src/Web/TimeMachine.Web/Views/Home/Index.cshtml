@model TimeMachine.Web.Models.Home.IndexViewModel
@{
    ViewData["Title"] = "Home Page";
}

<div class="container">
	<div class="row">
		@foreach (var userProfile in Model.UserProfiles)
		{
			<div class="col">@userProfile.UserFullName</div>
		}
	</div>
</div>